dummy = ListNode(0, head)        dummy = ListNode(0, head)
        left = head        left = head
        prev = dummy        prev = dummy
        while left and left.next:        while left and left.next:
            right = left.next            right = left.next
            next_node = right.next            next_node = right.next

            prev.next = right                        prev.next = right            
            right.next = left            right.next = left
            left.next = next_node            left.next = next_node

            prev = left            prev = left
            left = left.next            left = left.next

        return dummy.next        return dummy.next